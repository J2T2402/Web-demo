# Caddy tự cấp & gia hạn SSL (Let's Encrypt), tự chuyển HTTP->HTTPS, tự đẩy WebSocket.
# Domain lấy từ biến môi trường DOMAIN (đặt trong .env). Caddy proxy về app qua mạng nội bộ.

{$DOMAIN} {
    reverse_proxy app:3000
}
